/** Public site URL — used for Open Graph tags. */
export const SITE_URL = 'https://subsecond.app';

export const CONTACT_EMAIL = 'hello@subsecond.app';

/**
 * Optional Calendly (or similar) booking URL. When set, a secondary link appears
 * on the contact section. Replace with your real link when ready.
 */
export const CALENDLY_URL = '';
